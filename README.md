# Video Shelf

A private YouTube video bookmark app for saving videos by category with notes.

## Run Locally

```bash
npm install
npm run dev
```

## Build

```bash
npm run build
```

Vercel can deploy this app with the default Vite settings:

- Build command: `npm run build`
- Output directory: `dist`
